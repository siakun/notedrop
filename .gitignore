# agent tools (claude code, codex)
# canonical: .agents/skills/ + AGENTS.md
# .claude/skills/ and CLAUDE.md are generated as junction/hard link by scripts/link-agent-files.ps1
.claude/
CLAUDE.md
# scripts/link-agent-files.ps1 가 hardlink 충돌 시 만드는 안전 backup — 추적 X
CLAUDE.md.bak.*

# build artifacts
main.js
main.js.map

# node
node_modules/
plugin/coverage/

# viewer build output
viewer/dist/

# viewer sample build output (generated by plugin/tools/generate-sample)
# sample 원본은 viewer/samples/ 에 git tracked, 빌드 결과는 추적 X
viewer/public/manifest.json
viewer/public/content/

# embedded viewer assets (regenerated each plugin build)
plugin/src/embedded/

# sample builder bundle (esbuild 산출, 매 실행마다 새로 빌드)
plugin/tools/generate-sample.mjs

# editor
.DS_Store
Thumbs.db
