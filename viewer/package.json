{
  "name": "@notedrop/viewer",
  "version": "0.1.15",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch --serve"
  },
  "dependencies": {
    "marked": "^12.0.2",
    "dompurify": "^3.1.6"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
